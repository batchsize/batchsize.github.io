# class

```bash
拷贝构造函数
Box(const Box& B) {
    l = B.l;
    b = B.b;
    h = B.h;
}
// 拷贝构造函数里直接访问私有成员比用 getter 简洁

operator< 参数
bool operator<(const Box& B) const
// 比较两个 Box 不应该修改任何一个对象，而且要支持 const
```


```bash
const 参数:
bool operator<(const Box& B) const;

const Box& B
这里 B 是一个引用参数。
const 表示在函数体内部 不能修改 B。
好处：
避免误操作修改传入的对象。
可以支持用 const 对象 作为参数：
const Box b1(1,2,3);
Box b2(4,5,6);
bool res = b2 < b1; // ✅ b1 是 const，也能传进去

... ) const  // 成员函数结尾的 const
表示这个成员函数 不会修改当前对象 (this) 的成员变量
在函数内部，this 的类型会变成 const Box*，所以你不能改 l, b, h。

好处：
保证比较函数是“只读”的。
允许你对 const Box 对象 调用这个函数：
const Box b1(1,2,3), b2(4,5,6);
bool res = (b1 < b2);  // ✅ 因为 operator< 是 const 成员函数


综合解释:
比较操作不会修改参数 B（第一个 const）。
比较操作不会修改当前对象 this（第二个 const）。
也就是说，这个 < 运算符是一个只读函数，既不会改“自己”，也不会改“别人”。

```


```bash
在 C++ 里，类的成员函数（包括构造函数、拷贝构造函数、析构函数、普通成员函数）都对本类的对象有“特权访问权限”。

一个类的成员函数，不仅能访问当前对象的私有成员(this->l)，
也能访问 同类的其他对象的私有成员。

class Box {
private:
    int l, b, h;

public:
    Box(int length, int breadth, int height) : l(length), b(breadth), h(height) {}

    // 拷贝构造函数
    Box(const Box& B) {
        // 这里虽然 B 是另一个对象，但因为它和 this 是同一个类，
        // 所以可以直接访问它的私有成员
        l = B.l;
        b = B.b;
        h = B.h;
    }

    void copyFrom(const Box& other) {
        // 普通成员函数里，也能直接访问 other 的私有成员
        l = other.l;
        b = other.b;
        h = other.h;
    }
};


访问控制是按类(class)级别，而不是按对象(instance)级别。
private 意味着“只能被这个类的成员函数访问”。

不管是访问 当前对象的私有变量，还是 同类对象的私有变量，只要在类的成员函数里，都算合法。


 `private / protected / public` 在类、对象、派生类之间的可见性。

---

## C++ 访问控制示意图

```
                ┌───────────────────────────────┐
                │            class A            │
                ├──────────────┬────────────────┤
                │   private     │  仅 A 的成员函数 │
                │   protected   │  A 及其子类成员 │
                │   public      │  任何地方都可访问 │
                └──────────────┴────────────────┘
                           │
                           │
          ┌────────────────┴─────────────────┐
          │                                  │
     同一个类的对象                  派生类 class B : public A
          │                                  │
   ┌──────┴───────┐                  ┌───────┴────────┐
   │              │                  │                │
   │ 可以访问 A 的     │              │ 可以访问 A 的   │
   │ private 成员      │              │ protected 成员  │
   │ (因为在 A 的成员函数里) │         │ (不能访问 A 的 private)│
   │                 │                │                │
   │ 外部函数/非友元  │                │    外部函数/非友元 │
   │ ────────────────┘                └────────────────┘
    只能访问 public 成员                只能访问 public 成员
```
```
---

### 解释：

1. **private**

   * 只能在 **类 A 自己的成员函数** 内访问。
   * 包括访问 **this 对象** 和 **其他 A 对象** 的 private 成员。

2. **protected**

   * 和 private 类似，但允许 **子类（派生类）** 访问。
   * 子类依然不能访问基类对象的 private 成员。

3. **public**

   * 任何地方都能访问（包括外部函数、main 等）。

---

### 举个代码例子

```cpp
class A {
private:
    int x;
protected:
    int y;
public:
    int z;

    void copyFrom(const A& other) {
        x = other.x;  // ✅ 可以访问同类对象的 private
        y = other.y;  // ✅ 可以访问同类对象的 protected
        z = other.z;  // ✅ 可以访问同类对象的 public
    }
};

class B : public A {
public:
    void test() {
        // x = 10;   ❌ 子类不能访问基类的 private
        y = 20;      // ✅ 子类可以访问基类的 protected
        z = 30;      // ✅ 子类可以访问基类的 public
    }
};

int main() {
    A a;
    // a.x = 1;    ❌ 外部无法访问 private
    // a.y = 2;    ❌ 外部无法访问 protected
    a.z = 3;       // ✅ 外部可以访问 public
}

```


```bash
friend ostream& operator<<(ostream& out, const Box& B) { out << B.l << " " << B.b << " " << B.h; return out; }

operator<< 是一个全局运算符重载（不是成员函数）。

它的第一个参数必须是 ostream&（比如 std::cout）。

如果写成成员函数，就变成 cout.operator<<(box)，这不直观。

所以我们通常把 operator<< 写成非成员函数，并加 friend，这样它可以直接访问类的私有成员。

👉 没有 friend 的话，就只能通过 getLength() 等 getter 来访问。


为什么返回 ostream&
return out;

这样可以支持 连续输出：
cout << box1 << box2 << box3;

如果不返回 ostream&，链式调用就不成立。
std::ostream 本身的 operator<< 也是这样设计的。


为什么 const Box& B
friend ostream& operator<<(ostream& out, const Box& B)
const：保证输出的时候不会修改 Box。
&：避免拷贝整个对象，提高效率。

friend：允许访问私有成员。
ostream&：保证链式输出。
const Box&：高效、只读。



1. operator< 需要比较两个对象
语法上你希望写：
if (box1 < box2) { ... }

如果是成员函数：
bool operator<(const Box& other) const;
那么等价于：
box1.operator<(box2);

很自然，左操作数 (box1) 是 this 对象，右操作数 (box2) 是参数。
这种情况正好符合 < 的语义：用 box1 去比较 box2。
所以写成成员函数就很方便。


operator<< 的左操作数是 ostream（cout）。
你没办法把它写成 ostream 的成员函数（你不能去改标准库 ostream）。
所以必须写成非成员函数，而为了访问 Box 的私有成员，就需要加 friend。
```



 **运算符重载写法对比表**，特别区分什么时候该用 **成员函数**、什么时候该用 **非成员函数**、什么时候需要 **friend**。

---

# 📌 C++ 运算符重载对比表

| 特点           | 成员函数方式                                                               | 非成员函数方式                                        | `friend` 非成员函数方式                             |
| ------------ | -------------------------------------------------------------------- | ---------------------------------------------- | -------------------------------------------- |
| **定义位置**     | 类内 `class A { T operator+(...); };`                                  | 类外 `T operator+(const A&, const A&);`          | 类内声明 + 类外定义：`friend T operator+(...);`       |
| **参数个数**     | 少一个参数（左操作数是 `this` 对象）                                               | 两个参数（左右操作数都要传）                                 | 两个参数，但可以直接访问类的私有成员                           |
| **能否访问私有成员** | ✅ 可以（本身就是类成员）                                                        | ❌ 不能（除非通过 `public` 接口）                         | ✅ 可以（因为被声明为 `friend`）                        |
| **常见用途**     | 改变对象内部状态的运算符，如 `operator=`, `operator[]`, `operator()`, `operator->` | 算术或比较运算符，当左右操作数对称时，如 `operator+`, `operator==` | 输入/输出流运算符（`<<`, `>>`），以及需要直接访问私有成员的比较运算符     |
| **调用方式**     | `a + b` → `a.operator+(b)`                                           | `a + b` → `operator+(a, b)`                    | `cout << a` → `operator<<(cout, a)`          |
| **是否允许左右对称** | ❌ 左操作数必须是类对象（或能隐式转换成类对象）                                             | ✅ 左右操作数都灵活                                     | ✅ 同非成员，但还能访问私有成员                             |
| **典型例子**     | `operator=`, `operator[]`, `operator()`                              | `operator+`, `operator-`, `operator==`         | `operator<<`, `operator>>`, 某些 `operator<` 等 |

---

## ✅ 直观总结：

* **成员函数**：当运算符本质上是“对象行为”，需要修改或访问当前对象内部时用（`=`, `[]`, `()`, `->`）。
* **非成员函数**：当运算符是两个对象之间的对称运算时用（`+`, `-`, `*`, `==`, `<`）。
* **friend 非成员函数**：当你既要对称运算，又要直接访问类的私有成员时用（最典型就是 `<<`、`>>`）。

---

## 📌 例子

### 成员函数

```cpp
class Box {
public:
    int l;
    Box& operator=(const Box& other) { l = other.l; return *this; }
};
```

### 非成员函数

```cpp
bool operator==(const Box& a, const Box& b) {
    return a.getLength() == b.getLength();
}
```

### friend 非成员函数

```cpp
class Box {
    int l, b, h;
public:
    friend ostream& operator<<(ostream& os, const Box& B) {
        os << B.l << " " << B.b << " " << B.h;
        return os;
    }
};
```

---

要不要我再帮你画一张 **流程图**，快速判断 “我现在要写的这个运算符到底该用哪种方式”？


```bash
字面量后缀
| 写法     | 类型 (至少)              | 说明                     |
| ------ | -------------------- | ---------------------- |
| `1`    | `int`                | 默认整型字面量                |
| `1U`   | `unsigned int`       | U 表示无符号                |
| `1L`   | `long`               | L 表示 long              |
| `1UL`  | `unsigned long`      | 无符号 long               |
| `1LL`  | `long long`          | LL 表示 long long        |
| `1ULL` | `unsigned long long` | U + LL = 无符号 long long |

字符和字符串字面量的类型前缀
| 写法        | 类型              |
| --------- | --------------- |
| `'a'`     | `char`          |
| `u'a'`    | `char16_t`      |
| `U'a'`    | `char32_t`      |
| `L'a'`    | `wchar_t`       |
| `"abc"`   | `const char[ ]` |
| `u8"abc"` | UTF-8 编码的字符串字面量 |

#include <iostream>
using namespace std;

int main() {
    cout << typeid('a').name() << endl;    // char
    cout << typeid(u'a').name() << endl;   // char16_t
    cout << typeid(U'a').name() << endl;   // char32_t
    cout << typeid(L'a').name() << endl;   // wchar_t

    cout << typeid("abc").name() << endl;  // const char[4]
    cout << typeid(u8"abc").name() << endl;// const char[4] 或 const char8_t[4] (C++20)
    cout << typeid(u"abc").name() << endl; // const char16_t[4]
    cout << typeid(U"abc").name() << endl; // const char32_t[4]
    cout << typeid(L"abc").name() << endl; // const wchar_t[4]
}

前缀作用：决定字符/字符串的存储编码和类型。

普通写法 'a'、"abc"：就是 char / const char[]。

u / U 前缀：专门为 Unicode（UTF-16 / UTF-32）设计。

L 前缀：传统的“宽字符”，跨平台时大小不同。

u8 前缀：UTF-8 编码（C++20 以后专用 char8_t 类型）。


| 写法     | 类型            |
| ------ | ------------- |
| `1.0`  | `double`（默认）  |
| `1.0f` | `float`       |
| `1.0F` | `float`       |
| `1.0l` | `long double` |
| `1.0L` | `long double` |

为什么要加后缀？

主要有两个目的：

避免溢出（比如 1LL * l * b * h 把计算提升到 long long）。

控制精度和类型（比如 3.14f 明确指定为 float，而不是 double）。

U = unsigned

L = long

LL = long long

f/F = float

l/L = long double

组合后缀表示对应类型（比如 ULL = unsigned long long）。

```



```bash
凡是加了 friend 的函数，即使写在类里面、即使有函数体，它也不会成为类的成员。

friend 函数永远是非成员函数，只是编译器允许它访问类的私有/保护成员

friend = 告诉编译器：“这玩意儿是外部函数，但给它开小灶，可以看我家私有变量”。
友元函数（类外定义，类内声明 friend）


friend ostream& operator<<(ostream& os, const Box& B) {
    os << B.l << " " << B.b << " " << B.h;
    return os;
}
友元函数可以在类内 直接定义（C++11 起合法），也可以 类外定义。
放类内时，编译器会当作 inline 的全局函数。
工程实践里：简单函数 → 类内定义；复杂函数 → 类外实现。


成员函数:

std::ostream& operator<<(std::ostream& os) const {
        os << a << "+i" << b;
        return os;
    }
const 修饰 隐式的 this 指针
调用：
Complex c(1,2);
c << cout;   // 顺序反了


在 C++ 预处理器里，可以用 # 来把宏参数转换为字符串。
#define toStr(x) #x


operator +
成员函数更常见，适合大多数二元运算符（+ - * / % 等）。
非成员函数更灵活，常见场景：
支持隐式类型转换，例如：（int + Complex 这种“左边不是类对象”的情况）
omplex c(1,2);
c = 5 + c;   // 需要非成员函数，否则 5 不是 Complex，没法调用成员 operator+

需要对称性强的运算，比如比较运算符 operator==，通常写成非成员函数。



*	指针声明 int* p	  取值（解引用） *p	    运算符重载/乘法 c1*c2
&	取地址 &a	      引用声明 int& r	   位运算 AND a & b

```